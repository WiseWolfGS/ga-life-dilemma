# 2025년 12월 22일 개발 로그

## 🎯 오늘의 목표
- 1차 개발 환경 구축
- MVP 사양 명확화 및 시뮬레이션 핵심 로직 구현
- 기본 시각화 및 인터랙션 구현

## ✅ 완료된 작업

### 1. pnpm 모노레포 환경 구축
- 기존 `npm` 기반 프로젝트를 `pnpm` 워크스페이스 기반의 모노레포로 전환했습니다.
- `apps/web` (프론트엔드)과 `packages/core` (핵심 로직)로 패키지를 분리하여 관심사를 분리하고 확장성을 확보했습니다.
- 모노레포 구조에 맞게 `.gitignore` 및 설계 문서(`recommended_structure.txt`, `mvp_checklist.md`)를 업데이트했습니다.

### 2. `@ga-life/core` 패키지 - 핵심 로직 구현
- **데이터 구조 정의**: `Grid`, `Cell`, `Gene`, `SimParams` 등 핵심 타입을 정의했습니다. (`sim/types.ts`, `config/schema.ts`)
- **초기화 로직**: 지정된 밀도에 따라 그리드를 무작위로 생성하는 `createGrid` 함수를 구현했습니다. (`sim/init.ts`)
- **영향력 계산**: 토러스 경계를 지원하는 `calculateInfluence` 함수를 구현하여 각 셀의 `S_total`을 계산했습니다. (`sim/influence.ts`)
- **확률 계산**: `README`의 수식을 기반으로 생존/탄생 확률을 계산하는 `calculateSurvivalProbability`, `calculateBirthProbability` 함수를 구현했습니다. (`sim/prob.ts`)
- **`step` 함수**: 위의 모든 로직을 통합하여 시뮬레이션을 한 세대 진행시키는 `step` 함수를 구현했습니다. (`sim/step.ts`)

### 3. `apps/web` 패키지 - 시각화 및 인터랙션 구현
- **`SimCanvas` 컴포넌트**: `Grid` 데이터를 받아 Canvas에 렌더링하는 컴포넌트를 구현했습니다.
- **`useSimulation` 훅**: 시뮬레이션 상태(`grid`, `params`)를 관리하고, `step` 함수를 호출하는 로직을 담은 커스텀 훅을 구현했습니다.
- **버그 수정 및 리팩토링**:
    - 초기: 자동 `requestAnimationFrame` 루프로 구현했으나, 모든 세포가 소멸하는 현상 발생.
    - 원인 분석: 버그가 아닌, 초기 파라미터 값이 너무 가혹하여 발생하는 현상임을 파악.
    - 해결: 사용자가 직접 규칙을 탐색할 수 있도록, 자동 루프를 제거하고 **수동 스텝 진행** 방식으로 리팩토링.
- **`ParamPanel` 컴포넌트**: `mu`, `nu`, `delta` 등 핵심 파라미터를 실시간으로 조절할 수 있는 UI를 구현했습니다.
- **최종 통합**: `page.tsx`에서 모든 컴포넌트와 훅을 조합하여, 파라미터 조절 및 'Next Step' 버튼을 통한 수동 시뮬레이션 환경을 완성했습니다.

## 📝 요약 및 다음 단계
- pnpm 모노레포 환경에서 핵심 로직과 UI의 관심사 분리를 성공적으로 완료했습니다.
- 시뮬레이션의 기본 파이프라인(초기화 → 계산 → 1세대 진행 → 렌더링)을 모두 구현하고, 사용자가 직접 규칙을 탐색할 수 있는 인터랙티브 MVP의 기반을 마련했습니다.
- 다음 단계는 `mvp_checklist.md`에 따라 유전 알고리즘(부모 선택, 교차, 변이)을 구현하거나, 뷰 모드 전환 등 UI 기능을 고도화하는 것이 될 수 있습니다.
