# GA-Life 프로젝트 주요 명령어 모음

이 문서는 프로젝트 개발 및 관리에 사용되는 주요 명령어들을 정리합니다.

---

## 🌳 프로젝트 구조 확인

### `structure.txt` 파일 갱신
프로젝트의 현재 디렉토리 구조를 `structure.txt` 파일에 저장합니다.

```bash
tree -I "node_modules|dist|build|.git|.next|coverage" -L 5 > docs/structure/current_structure.txt
```

---

## 📦 의존성 관리

### 전체 의존성 설치
모든 워크스페이스(frontend, backend, packages)의 의존성을 설치합니다.

```bash
pnpm install
```

---
